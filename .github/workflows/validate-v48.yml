name: Validate cookie domain ui v48
on:
  push:
    branches: [feat/cookie-domain-ui-v48]
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build APK
        env:
          WEB_RESEARCH_VERSION: v48-validation
        run: gradle :app:assembleDebug
