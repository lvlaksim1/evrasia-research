name: Apply result delivery refactor
on:
  push:
    branches: [result-delivery-work]
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: result-delivery-work
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Apply refactor
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          root = Path('app/src/main/java/ru/evrasia/research')

          result_delivery = r'''package ru.evrasia.research

import android.app.Activity
import android.app.Dialog
import android.content.ClipData
import android.content.ClipboardManager
import android.content.ContentValues
import android.content.Intent
import android.graphics.Color
import android.graphics.Typeface
import android.graphics.drawable.ColorDrawable
import android.graphics.drawable.GradientDrawable
import android.os.Build
import android.os.Environment
import android.provider.MediaStore
import android.view.Gravity
import android.view.WindowManager
import android.widget.Button
import android.widget.LinearLayout
import android.widget.TextView
import android.widget.Toast
import androidx.core.content.FileProvider
import java.io.File
import java.io.OutputStream
import java.text.SimpleDateFormat
import java.util.Date
import java.util.Locale
import java.util.WeakHashMap

internal object ResultDelivery {
    private const val REQUEST_SAVE = 7901

    private data class Prepared(
        val title: String,
        val file: File,
        val mime: String,
        val clipboardText: String?
    )

    private val pendingSave = WeakHashMap<Activity, Prepared>()

    fun deliverText(
        activity: Activity,
        title: String,
        text: String,
        fileName: String = defaultFileName(title, text),
        mime: String = guessMime(title, text)
    ) {
        val prepared = prepare(activity, title, fileName, mime, text) { output ->
            output.write(text.toByteArray(Charsets.UTF_8))
        } ?: return
        showChoice(activity, prepared)
    }

    fun deliverBytes(activity: Activity, title: String, bytes: ByteArray, fileName: String, mime: String) {
        val prepared = prepare(activity, title, fileName, mime.ifBlank { "application/octet-stream" }, null) { output ->
            output.write(bytes)
        } ?: return
        showChoice(activity, prepared)
    }

    fun deliverGeneratedFile(
        activity: Activity,
        title: String,
        fileName: String,
        mime: String,
        writer: (OutputStream) -> Unit
    ) {
        val prepared = prepare(activity, title, fileName, mime, null, writer) ?: return
        showChoice(activity, prepared)
    }

    fun defaultFileName(label: String, value: String = ""): String {
        val stamp = SimpleDateFormat("yyyyMMdd-HHmmss", Locale.US).format(Date())
        val base = label.lowercase(Locale.US)
            .replace(Regex("[^a-z0-9а-яё._-]+", RegexOption.IGNORE_CASE), "-")
            .trim('-').ifBlank { "result" }
        val ext = when {
            label.contains("postman", true) || label.contains("json", true) || value.trimStart().startsWith("{") || value.trimStart().startsWith("[") -> "json"
            label.contains("javascript", true) || label.contains("js setter", true) -> "js"
            label.contains("curl", true) -> "sh"
            else -> "txt"
        }
        return "$base-$stamp.$ext"
    }

    fun handleActivityResult(activity: Activity, requestCode: Int, resultCode: Int, data: Intent?): Boolean {
        if (requestCode != REQUEST_SAVE) return false
        val prepared = synchronized(pendingSave) { pendingSave.remove(activity) } ?: return true
        if (resultCode != Activity.RESULT_OK) return true
        val uri = data?.data ?: return true
        return try {
            activity.contentResolver.openOutputStream(uri)?.use { output -> prepared.file.inputStream().use { input -> input.copyTo(output) } }
            Toast.makeText(activity, "Файл сохранён", Toast.LENGTH_SHORT).show()
            true
        } catch (_: Exception) {
            Toast.makeText(activity, "Не удалось сохранить файл", Toast.LENGTH_LONG).show()
            true
        }
    }

    private fun prepare(
        activity: Activity,
        title: String,
        fileName: String,
        mime: String,
        clipboardText: String?,
        writer: (OutputStream) -> Unit
    ): Prepared? {
        return try {
            val folder = File(activity.cacheDir, "exports").apply { mkdirs() }
            val safeName = sanitizeFileName(fileName)
            val file = File(folder, safeName)
            file.outputStream().use(writer)
            Prepared(title, file, mime.ifBlank { "application/octet-stream" }, clipboardText)
        } catch (_: Exception) {
            Toast.makeText(activity, "Не удалось подготовить результат", Toast.LENGTH_LONG).show()
            null
        }
    }

    private fun showChoice(activity: Activity, prepared: Prepared) {
        val palette = WebUiTheme.palette(activity)
        val dialog = Dialog(activity)
        dialog.setCanceledOnTouchOutside(false)
        dialog.setCancelable(true)

        val root = LinearLayout(activity).apply {
            orientation = LinearLayout.VERTICAL
            setPadding(dp(activity, 10), dp(activity, 10), dp(activity, 10), dp(activity, 12))
            background = rounded(activity, palette.card, 20f, palette.divider)
        }
        val header = LinearLayout(activity).apply {
            orientation = LinearLayout.HORIZONTAL
            gravity = Gravity.CENTER_VERTICAL
        }
        header.addView(squareButton(activity, palette, "×") { dialog.dismiss() }, LinearLayout.LayoutParams(dp(activity, 40), dp(activity, 40)))
        header.addView(TextView(activity).apply {
            text = prepared.title
            setTextColor(palette.text)
            textSize = 16f
            typeface = Typeface.DEFAULT_BOLD
            gravity = Gravity.CENTER_VERTICAL
            setPadding(dp(activity, 9), 0, 0, 0)
        }, LinearLayout.LayoutParams(0, dp(activity, 40), 1f))
        root.addView(header)
        root.addView(TextView(activity).apply {
            text = prepared.file.name
            setTextColor(palette.secondary)
            textSize = 11.5f
            setPadding(dp(activity, 8), dp(activity, 8), dp(activity, 8), dp(activity, 12))
        })

        root.addView(actionButton(activity, palette, "Скачать файл", true) {
            if (download(activity, prepared)) dialog.dismiss()
        }, LinearLayout.LayoutParams(-1, dp(activity, 48)).apply { setMargins(dp(activity, 6), 0, dp(activity, 6), dp(activity, 6)) })

        root.addView(actionButton(activity, palette, "Отправить в другое приложение", false) {
            share(activity, prepared)
            dialog.dismiss()
        }, LinearLayout.LayoutParams(-1, dp(activity, 48)).apply { setMargins(dp(activity, 6), 0, dp(activity, 6), dp(activity, 6)) })

        root.addView(actionButton(activity, palette, "Копировать в буфер", false) {
            copy(activity, prepared)
            dialog.dismiss()
        }, LinearLayout.LayoutParams(-1, dp(activity, 48)).apply { setMargins(dp(activity, 6), 0, dp(activity, 6), 0) })

        dialog.setContentView(root)
        dialog.window?.setBackgroundDrawable(ColorDrawable(Color.TRANSPARENT))
        dialog.setOnShowListener {
            dialog.window?.apply {
                setBackgroundDrawable(ColorDrawable(Color.TRANSPARENT))
                setLayout((activity.resources.displayMetrics.widthPixels * 0.92f).toInt(), WindowManager.LayoutParams.WRAP_CONTENT)
                setGravity(Gravity.CENTER)
            }
        }
        dialog.show()
    }

    private fun download(activity: Activity, prepared: Prepared): Boolean {
        if (Build.VERSION.SDK_INT < Build.VERSION_CODES.Q) {
            synchronized(pendingSave) { pendingSave[activity] = prepared }
            activity.startActivityForResult(Intent(Intent.ACTION_CREATE_DOCUMENT).apply {
                addCategory(Intent.CATEGORY_OPENABLE)
                type = prepared.mime
                putExtra(Intent.EXTRA_TITLE, prepared.file.name)
            }, REQUEST_SAVE)
            return true
        }
        return try {
            val values = ContentValues().apply {
                put(MediaStore.Downloads.DISPLAY_NAME, prepared.file.name)
                put(MediaStore.Downloads.MIME_TYPE, prepared.mime)
                put(MediaStore.Downloads.RELATIVE_PATH, Environment.DIRECTORY_DOWNLOADS)
                put(MediaStore.Downloads.IS_PENDING, 1)
            }
            val resolver = activity.contentResolver
            val uri = resolver.insert(MediaStore.Downloads.EXTERNAL_CONTENT_URI, values) ?: return false
            resolver.openOutputStream(uri)?.use { output -> prepared.file.inputStream().use { input -> input.copyTo(output) } }
            values.clear()
            values.put(MediaStore.Downloads.IS_PENDING, 0)
            resolver.update(uri, values, null, null)
            Toast.makeText(activity, "Файл сохранён в Загрузки", Toast.LENGTH_SHORT).show()
            true
        } catch (_: Exception) {
            Toast.makeText(activity, "Не удалось сохранить файл", Toast.LENGTH_LONG).show()
            false
        }
    }

    private fun share(activity: Activity, prepared: Prepared) {
        try {
            val uri = FileProvider.getUriForFile(activity, "${activity.packageName}.files", prepared.file)
            val send = Intent(Intent.ACTION_SEND).apply {
                type = prepared.mime
                putExtra(Intent.EXTRA_STREAM, uri)
                clipData = ClipData.newUri(activity.contentResolver, prepared.title, uri)
                addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION)
            }
            activity.startActivity(Intent.createChooser(send, "Отправить ${prepared.title}"))
        } catch (_: Exception) {
            Toast.makeText(activity, "Не удалось открыть меню отправки", Toast.LENGTH_LONG).show()
        }
    }

    private fun copy(activity: Activity, prepared: Prepared) {
        val manager = activity.getSystemService(Activity.CLIPBOARD_SERVICE) as ClipboardManager
        if (prepared.clipboardText != null) {
            manager.setPrimaryClip(ClipData.newPlainText(prepared.title, prepared.clipboardText))
            Toast.makeText(activity, "${prepared.title} скопирован", Toast.LENGTH_SHORT).show()
        } else {
            try {
                val uri = FileProvider.getUriForFile(activity, "${activity.packageName}.files", prepared.file)
                manager.setPrimaryClip(ClipData.newUri(activity.contentResolver, prepared.title, uri))
                Toast.makeText(activity, "Файл скопирован в буфер", Toast.LENGTH_SHORT).show()
            } catch (_: Exception) {
                Toast.makeText(activity, "Не удалось скопировать файл", Toast.LENGTH_LONG).show()
            }
        }
    }

    private fun guessMime(label: String, value: String): String = when {
        label.contains("postman", true) || label.contains("json", true) || value.trimStart().startsWith("{") || value.trimStart().startsWith("[") -> "application/json"
        label.contains("javascript", true) || label.contains("js setter", true) -> "application/javascript"
        else -> "text/plain"
    }

    private fun sanitizeFileName(name: String): String {
        val clean = name.replace(Regex("[\\/:*?\"<>|]+"), "-").trim().trim('.')
        return clean.ifBlank { defaultFileName("result") }.take(180)
    }

    private fun squareButton(activity: Activity, palette: WebUiTheme.Palette, symbol: String, click: () -> Unit) = Button(activity).apply {
        text = symbol
        setTextColor(palette.accent)
        textSize = 19f
        isAllCaps = false
        minWidth = 0
        minimumWidth = 0
        minHeight = 0
        minimumHeight = 0
        setPadding(0, 0, 0, 0)
        background = rounded(activity, palette.address, 11f, palette.divider)
        setOnClickListener { click() }
    }

    private fun actionButton(activity: Activity, palette: WebUiTheme.Palette, label: String, primary: Boolean, click: () -> Unit) = Button(activity).apply {
        text = label
        isAllCaps = false
        textSize = 13f
        typeface = if (primary) Typeface.DEFAULT_BOLD else Typeface.DEFAULT
        setTextColor(if (primary) WebUiTheme.contrastText(palette.accent) else palette.text)
        background = rounded(activity, if (primary) palette.accent else palette.address, 14f, if (primary) palette.accent else palette.divider)
        setOnClickListener { click() }
    }

    private fun rounded(activity: Activity, fill: Int, radius: Float, stroke: Int = Color.TRANSPARENT) = GradientDrawable().apply {
        shape = GradientDrawable.RECTANGLE
        setColor(fill)
        cornerRadius = dp(activity, radius.toInt()).toFloat()
        if (stroke != Color.TRANSPARENT) setStroke(dp(activity, 1), stroke)
    }

    private fun dp(activity: Activity, value: Int) = (value * activity.resources.displayMetrics.density).toInt()
}
'''
          (root / 'ResultDelivery.kt').write_text(result_delivery, encoding='utf-8')

          postman = r'''package ru.evrasia.research

import android.app.Activity
import java.text.SimpleDateFormat
import java.util.Date
import java.util.Locale

internal object PostmanDelivery {
    fun deliver(activity: Activity, json: String, sourceUrl: String = "") {
        val host = try {
            java.net.URL(sourceUrl).host.replace(Regex("[^A-Za-z0-9._-]"), "-").ifBlank { "request" }
        } catch (_: Exception) {
            "request"
        }
        val stamp = SimpleDateFormat("yyyyMMdd-HHmmss", Locale.US).format(Date())
        ResultDelivery.deliverText(activity, "POSTMAN JSON", json, "postman-$host-$stamp.json", "application/json")
    }
}
'''
          (root / 'PostmanDelivery.kt').write_text(postman, encoding='utf-8')

          def load(name):
              p = root / name
              return p, p.read_text(encoding='utf-8')

          def save(p, text):
              p.write_text(text, encoding='utf-8')

          # Main browser: remove global Postman mode and route cookie output through unified actions.
          p, text = load('WebResearchV10Activity.kt')
          text, count = re.subn(r'\n            addMenuRow\("\{ \}", "POSTMAN JSON", "Режим: \$\{PostmanDelivery\.modeLabel\(this@WebResearchV10Activity\)\}"\) \{\n                dialog\.dismiss\(\)\n                showPostmanModePicker\(\)\n            \}\n', '\n', text, count=1)
          if count != 1:
              raise SystemExit('POSTMAN hamburger row not found')
          old_cookie = '''                addPrimaryButton("Копировать cookies") {
                    val manager = getSystemService(Context.CLIPBOARD_SERVICE) as ClipboardManager
                    manager.setPrimaryClip(ClipData.newPlainText("cookies", raw))
                    Toast.makeText(this@WebResearchV10Activity, "Cookies скопированы", Toast.LENGTH_SHORT).show()
                }'''
          new_cookie = '''                addPrimaryButton("Действия с cookies") {
                    ResultDelivery.deliverText(this@WebResearchV10Activity, "Cookies", raw, ResultDelivery.defaultFileName("cookies-${currentHost()}", raw), "text/plain")
                }'''
          if old_cookie not in text:
              raise SystemExit('cookie clipboard block not found')
          text = text.replace(old_cookie, new_cookie, 1)
          text, count = re.subn(r'\n    private fun showPostmanModePicker\(\) \{.*?\n    \}\n\n    private fun showThemePicker\(\)', '\n\n    private fun showThemePicker()', text, count=1, flags=re.S)
          if count != 1:
              raise SystemExit('showPostmanModePicker not found')
          save(p, text)

          # Modern network screen.
          p, text = load('NetworkResearchActivity.kt')
          old_copy = '''    private fun copy(label: String, value: String) {
        val manager = getSystemService(Context.CLIPBOARD_SERVICE) as ClipboardManager
        manager.setPrimaryClip(ClipData.newPlainText(label, value))
        Toast.makeText(this, "$label скопирован", Toast.LENGTH_SHORT).show()
    }'''
          new_copy = '''    private fun copy(label: String, value: String) {
        ResultDelivery.deliverText(this, label, value)
    }'''
          if old_copy not in text:
              raise SystemExit('NetworkResearch copy not found')
          text = text.replace(old_copy, new_copy, 1)
          text = text.replace('secondaryButton("Копировать URL") { copy("URL", url) }', 'secondaryButton("URL") { copy("URL", url) }', 1)
          old_export = '''    private fun beginExport() {
        NetworkRequestActions.prepareFullExport(this)
        val stamp = SimpleDateFormat("yyyyMMdd-HHmmss", Locale.US).format(Date())
        pendingExport = true
        startActivityForResult(Intent(Intent.ACTION_CREATE_DOCUMENT).apply {
            addCategory(Intent.CATEGORY_OPENABLE)
            type = "application/zip"
            putExtra(Intent.EXTRA_TITLE, "web-research-$stamp.zip")
        }, 801)
    }

    @Deprecated("Deprecated in Java")
    override fun onActivityResult(requestCode: Int, resultCode: Int, data: Intent?) {
        super.onActivityResult(requestCode, resultCode, data)
        if (requestCode != 801 || resultCode != RESULT_OK || !pendingExport) return
        pendingExport = false
        data?.data?.let { uri ->
            contentResolver.openOutputStream(uri)?.use { output -> NetworkRequestActions.writeFullExport(this, output) }
        }
        Toast.makeText(this, "ZIP экспортирован", Toast.LENGTH_SHORT).show()
    }'''
          new_export = '''    private fun beginExport() {
        NetworkRequestActions.prepareFullExport(this)
        val stamp = SimpleDateFormat("yyyyMMdd-HHmmss", Locale.US).format(Date())
        ResultDelivery.deliverGeneratedFile(this, "Экспорт ZIP", "web-research-$stamp.zip", "application/zip") { output ->
            if (!NetworkRequestActions.writeFullExport(this, output)) throw IllegalStateException("export failed")
        }
    }

    @Deprecated("Deprecated in Java")
    override fun onActivityResult(requestCode: Int, resultCode: Int, data: Intent?) {
        super.onActivityResult(requestCode, resultCode, data)
        ResultDelivery.handleActivityResult(this, requestCode, resultCode, data)
    }'''
          if old_export not in text:
              raise SystemExit('NetworkResearch export block not found')
          text = text.replace(old_export, new_export, 1)
          save(p, text)

          # Full network debugger: all clipboard and file actions route through ResultDelivery.
          p, text = load('NetworkDebuggerActivity.kt')
          old_copy_debug = '''    private fun copyText(label:String,value:String){
        val manager=getSystemService(Context.CLIPBOARD_SERVICE) as ClipboardManager
        manager.setPrimaryClip(ClipData.newPlainText(label,value));Toast.makeText(this,"$label скопирован",Toast.LENGTH_SHORT).show()
    }'''
          new_copy_debug = '''    private fun copyText(label:String,value:String){
        ResultDelivery.deliverText(this,label,value)
    }'''
          if old_copy_debug not in text:
              raise SystemExit('NetworkDebugger copyText not found')
          text = text.replace(old_copy_debug, new_copy_debug, 1)
          old_binary = '''    private fun beginBinarySave(event:JSONObject,bytes:ByteArray,mime:String){
        pendingBinary=bytes;pendingBinaryMime=mime.ifBlank{"application/octet-stream"};pendingBinaryName=suggestFileName(event,pendingBinaryMime)
        startActivityForResult(Intent(Intent.ACTION_CREATE_DOCUMENT).apply{addCategory(Intent.CATEGORY_OPENABLE);type=pendingBinaryMime;putExtra(Intent.EXTRA_TITLE,pendingBinaryName)},702)
    }'''
          new_binary = '''    private fun beginBinarySave(event:JSONObject,bytes:ByteArray,mime:String){
        val safeMime=mime.ifBlank{"application/octet-stream"}
        ResultDelivery.deliverBytes(this,"Ответ",bytes,suggestFileName(event,safeMime),safeMime)
    }'''
          if old_binary not in text:
              raise SystemExit('NetworkDebugger binary save not found')
          text = text.replace(old_binary, new_binary, 1)
          old_zip = '''    private fun exportZip(){
        NetworkRequestActions.prepareFullExport(this)
        val stamp=SimpleDateFormat("yyyyMMdd-HHmmss",Locale.US).format(Date())
        startActivityForResult(Intent(Intent.ACTION_CREATE_DOCUMENT).apply{addCategory(Intent.CATEGORY_OPENABLE);type="application/zip";putExtra(Intent.EXTRA_TITLE,"web-research-$stamp.zip")},701)
    }'''
          new_zip = '''    private fun exportZip(){
        NetworkRequestActions.prepareFullExport(this)
        val stamp=SimpleDateFormat("yyyyMMdd-HHmmss",Locale.US).format(Date())
        ResultDelivery.deliverGeneratedFile(this,"Экспорт ZIP","web-research-$stamp.zip","application/zip"){out->
            if(!NetworkRequestActions.writeFullExport(this,out))writeTraceFallback(out)
        }
    }'''
          if old_zip not in text:
              raise SystemExit('NetworkDebugger exportZip not found')
          text = text.replace(old_zip, new_zip, 1)
          old_result = '        super.onActivityResult(requestCode,resultCode,data);if(resultCode!=RESULT_OK)return'
          new_result = '        super.onActivityResult(requestCode,resultCode,data);if(ResultDelivery.handleActivityResult(this,requestCode,resultCode,data))return;if(resultCode!=RESULT_OK)return'
          if old_result not in text:
              raise SystemExit('NetworkDebugger onActivityResult head not found')
          text = text.replace(old_result, new_result, 1)
          text = text.replace('compactButton("Копировать"){copyText("REALTIME",copyText.toString().trim())}', 'compactButton("REALTIME"){copyText("REALTIME",copyText.toString().trim())}', 1)
          save(p, text)

          # Cookie tracing actions.
          p, text = load('CookieTraceUiProvider.kt')
          old_cookie_copy = '''    private fun copyText(activity: Activity, label: String, value: String) {
        val cm = activity.getSystemService(Context.CLIPBOARD_SERVICE) as ClipboardManager
        cm.setPrimaryClip(ClipData.newPlainText(label, value))
        Toast.makeText(activity, "$label скопирован", Toast.LENGTH_SHORT).show()
    }'''
          new_cookie_copy = '''    private fun copyText(activity: Activity, label: String, value: String) {
        ResultDelivery.deliverText(activity, label, value)
    }'''
          if old_cookie_copy not in text:
              raise SystemExit('CookieTrace copyText not found')
          text = text.replace(old_cookie_copy, new_cookie_copy, 1)
          text = text.replace('"КОПИРОВАТЬ cURL"', '"cURL"')
          text = text.replace('"КОПИРОВАТЬ JS SETTER"', '"JS SETTER"')
          save(p, text)

          export_controller = r'''package ru.evrasia.research

import android.content.Intent
import android.webkit.WebView
import androidx.appcompat.app.AppCompatActivity
import java.text.SimpleDateFormat
import java.util.Date
import java.util.Locale

internal class WebResearchExportController(
    private val activity: AppCompatActivity,
    private val archive: ResearchArchive,
    private val web: WebView,
    private val captureSnapshot: () -> Unit
) {
    fun start() {
        captureSnapshot()
        val stamp = SimpleDateFormat("yyyyMMdd-HHmmss", Locale.US).format(Date())
        ResultDelivery.deliverGeneratedFile(activity, "Экспорт ZIP", "web-research-$stamp.zip", "application/zip") { output ->
            archive.writeZip(output, web.url ?: "")
        }
    }

    fun handleResult(requestCode: Int, resultCode: Int, data: Intent?): Boolean =
        ResultDelivery.handleActivityResult(activity, requestCode, resultCode, data)
}
'''
          (root / 'WebResearchExportController.kt').write_text(export_controller, encoding='utf-8')

          paths = Path('app/src/main/res/xml/file_paths.xml')
          paths.write_text('''<?xml version="1.0" encoding="utf-8"?>\n<paths xmlns:android="http://schemas.android.com/apk/res/android">\n    <cache-path name="postman" path="postman/" />\n    <cache-path name="exports" path="exports/" />\n</paths>\n''', encoding='utf-8')
          PY
      - name: Verify no global Postman mode remains
        run: |
          ! grep -R "PostmanDelivery.mode\|PostmanDelivery.saveMode\|KEY_MODE" app/src/main/java/ru/evrasia/research
          grep -R "ResultDelivery.deliverText" app/src/main/java/ru/evrasia/research >/dev/null
      - name: Build APK on work branch
        run: gradle :app:assembleDebug
      - name: Commit verified refactor
        run: |
          rm .github/workflows/result-delivery-refactor.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: unify copy and file delivery [skip ci]"
          git push origin HEAD:result-delivery-work
